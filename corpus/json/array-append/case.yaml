name: json/array-append
seams: [e2e]
kind: ok
format: json
ops: [OP-11]
why: |
  A `+` element with a preceding context sibling and nothing after it appends.
  Note the context line is the LAST existing element, which is what makes this an
  append rather than an insert.
spec: "§6.2, §11 OP-11"
