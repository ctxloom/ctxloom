hew: 1

--- target.tf format=hcl

@@ / @@
! match label=["aws"] ord=0
  provider "aws" {
-   region  = "us-west-1"
+   region  = "us-west-2"
    profile = "default"
  }
! match label=["aws"] ord=1
  provider "aws" {
    alias  = "east"
    region = "us-east-1"
+   profile = "ctxloom"
  }
