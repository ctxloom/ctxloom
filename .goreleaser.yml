# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: ctxloom

before:
  hooks:
    - go mod tidy
    - go run ./scripts/genman

builds:
  # Standard build - includes memory and vectors (no CGO required)
  # Recommended for most users
  - id: ctxloom
    main: ./main.go
    binary: ctxloom
    tags:
      - memory
      - vectors
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/ctxloom/ctxloom/cmd.Version={{.Version}}

  # Full build - includes memory, vectors, and tree-sitter (31MB)
  # Requires CGO and cross-compilers. Statically linked on linux/windows (see
  # ldflags below); netgo/osusergo force the pure-Go resolver and user lookups
  # so static glibc linking doesn't break DNS or os/user.
  - id: ctxloom-full
    main: ./main.go
    binary: ctxloom
    tags:
      - memory
      - vectors
      - treesitter
      - netgo
      - osusergo
    env:
      - CGO_ENABLED=1
      # Cross-compiler environment variables (provided by goreleaser-cross image)
      - >-
        {{- if eq .Os "linux" }}
          {{- if eq .Arch "amd64" }}CC=x86_64-linux-gnu-gcc{{- end }}
          {{- if eq .Arch "arm64" }}CC=aarch64-linux-gnu-gcc{{- end }}
        {{- end }}
        {{- if eq .Os "darwin" }}
          {{- if eq .Arch "amd64" }}CC=o64-clang{{- end }}
          {{- if eq .Arch "arm64" }}CC=oa64-clang{{- end }}
        {{- end }}
        {{- if eq .Os "windows" }}
          {{- if eq .Arch "amd64" }}CC=x86_64-w64-mingw32-gcc{{- end }}
          {{- if eq .Arch "arm64" }}CC=/llvm-mingw/bin/aarch64-w64-mingw32-gcc{{- end }}
        {{- end }}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/ctxloom/ctxloom/cmd.Version={{.Version}}
      # Static linking for linux/windows; macOS cannot fully static-link system
      # libraries, so leave darwin dynamic.
      - '{{ if ne .Os "darwin" }}-extldflags "-static"{{ end }}'

# UPX compression for smaller binaries
# NOTE: UPX is disabled for darwin - it breaks code signing and causes
# "zsh: Killed" on macOS (especially Apple Silicon)
upx:
  - ids: [ctxloom]
    enabled: true
    goos: [linux, windows]
    goarch: [amd64, arm64]
    compress: best
    lzma: true
  - ids: [ctxloom-full]
    enabled: true
    goos: [linux, windows]
    goarch: [amd64, arm64]
    compress: best
    lzma: true

archives:
  # Light build archives (default, recommended)
  - id: default
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    ids:
      - ctxloom
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - LICENSE*
      - README.md
      - man/man1/*.1

  # Full build archives (with tree-sitter)
  - id: full
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    ids:
      - ctxloom-full
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}_full
    files:
      - LICENSE*
      - README.md
      - man/man1/*.1

# Homebrew casks published to ctxloom/homebrew-tap. Two casks mirror the two
# builds; they install the same `ctxloom` binary, so each declares a conflict
# with the other. Casks are macOS-only — Linux users install via scripts/install.sh.
#
# Requires the HOMEBREW_TAP_GITHUB_TOKEN secret (a PAT with push access to the
# tap repo) — the per-repo GITHUB_TOKEN cannot push to another repository.
#
# The post-install hook strips the macOS quarantine attribute so the unsigned
# binary launches without a Gatekeeper prompt.
homebrew_casks:
  # Standard build (recommended): memory + simple vector search, no CGO deps.
  #   brew install ctxloom/tap/ctxloom
  - name: ctxloom
    ids:
      - default
    binary: ctxloom
    repository:
      owner: ctxloom
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Casks
    homepage: "https://github.com/ctxloom/ctxloom"
    description: "Context Loom - Weave context for AI coding agents"
    license: "BSD-3-Clause"
    conflicts:
      - cask: ctxloom-full
    hooks:
      post:
        install: |
          if system_command("/usr/bin/xattr", args: ["-h"]).exit_status == 0
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/ctxloom"]
          end

  # Full build: adds tree-sitter AST code compression (CGO).
  #   brew install ctxloom/tap/ctxloom-full
  - name: ctxloom-full
    ids:
      - full
    binary: ctxloom
    repository:
      owner: ctxloom
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Casks
    homepage: "https://github.com/ctxloom/ctxloom"
    description: "Context Loom (full build with tree-sitter AST code compression)"
    license: "BSD-3-Clause"
    conflicts:
      - cask: ctxloom
    hooks:
      post:
        install: |
          if system_command("/usr/bin/xattr", args: ["-h"]).exit_status == 0
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/ctxloom"]
          end

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: ctxloom
    name: ctxloom
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
  header: |
    ## ctxloom v{{.Version}}

    Context Loom - Weave context for AI coding agents.

    ### Quick Install

    **macOS / Linux:**
    ```bash
    curl -fsSL https://raw.githubusercontent.com/ctxloom/ctxloom/main/scripts/install.sh | bash
    ```

    **Windows (PowerShell):**
    ```powershell
    irm https://raw.githubusercontent.com/ctxloom/ctxloom/main/scripts/install.ps1 | iex
    ```

    Both scripts also install the companion tools [taskloom](https://github.com/ctxloom/taskloom)
    (task tracking) and [ltk](https://github.com/ctxloom/llm-tool-killer) (command
    guardrails); opt out with `--no-companions` / `-NoCompanions`.

    **macOS (Homebrew), everything:**
    ```bash
    brew install ctxloom/tap/{ctxloom,taskloom,ltk}
    ```

    ### Which Build?

    | Build | Size | Features |
    |-------|------|----------|
    | **Standard** (`ctxloom_*`) | ~10MB | Memory, vector search (simple embedder) |
    | **Full** (`ctxloom_*_full`) | ~12MB | + Tree-sitter AST code compression |

    Both builds include memory logging and semantic search.
    The full build adds AST-based code compression (requires CGO).

    ### Manual Installation

    **Go Install:**
    ```bash
    go install github.com/ctxloom/ctxloom@v{{.Version}}
    ```

    **Manual Download:**
    Download the appropriate archive for your platform below.

    **Man Pages:**
    Man pages are included in the archive. Install with:
    ```bash
    sudo cp man/man1/*.1 /usr/local/share/man/man1/
    ```

    ### Security Verification

    Install script checksums (SHA256):
    - `install.sh`: `{{ .Env.HASH_INSTALL_SH }}`
    - `install.ps1`: `{{ .Env.HASH_INSTALL_PS1 }}`

    VirusTotal scans:
    - [install.sh](https://www.virustotal.com/gui/file/{{ .Env.HASH_INSTALL_SH }})
    - [install.ps1](https://www.virustotal.com/gui/file/{{ .Env.HASH_INSTALL_PS1 }})
