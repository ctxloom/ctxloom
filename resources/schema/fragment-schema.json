{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mlcm.dev/schemas/fragment.json",
  "title": "MLCM Context Fragment",
  "description": "Schema for MLCM context fragment YAML files",
  "type": "object",
  "required": ["content"],
  "properties": {
    "version": {
      "type": "string",
      "description": "Schema version for the fragment format",
      "examples": ["1.0"]
    },
    "author": {
      "type": "string",
      "description": "Who created or maintains this fragment",
      "examples": ["username", "team-name"]
    },
    "tags": {
      "type": "array",
      "description": "Categorization tags for filtering and persona assembly",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "examples": [["golang", "code-style"], ["review", "security"]]
    },
    "notes": {
      "type": "string",
      "description": "Human-readable documentation (NOT sent to AI). Use for usage guidance, caveats, when to use/avoid."
    },
    "content": {
      "type": "string",
      "description": "The context content sent to the AI. Markdown format. Supports {{variable}} substitution.",
      "minLength": 1
    },
    "content_hash": {
      "type": "string",
      "description": "SHA256 hash of content for change detection. Format: sha256:<hex>",
      "pattern": "^sha256:[a-f0-9]{64}$",
      "examples": ["sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"]
    },
    "distilled": {
      "type": "string",
      "description": "Compressed/distilled version of content that preserves meaning with fewer tokens. Generated by 'mlcm distill'."
    },
    "distilled_by": {
      "type": "string",
      "description": "The LLM model that performed the distillation (e.g., 'claude-3-opus', 'gpt-4').",
      "examples": ["claude-3-opus", "claude-3-sonnet", "gpt-4"]
    },
    "no_distill": {
      "type": "boolean",
      "description": "If true, this fragment should not be distilled. Use for content that must remain exactly as written.",
      "default": false
    },
    "exports": {
      "type": "object",
      "description": "Exported variable values from generators. Maps variable names to their values.",
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false
}
