---
title: "ltk completion zsh"
---
<!-- GENERATED by `just gen-docs` from the command definitions in cmd/ltk.
     Do not edit; edit the cobra Short/Long/Example fields instead. -->
:::note
This page is generated from `ltk completion zsh --help`.
:::

## ltk completion zsh

Generate the autocompletion script for zsh

### Synopsis

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need
to enable it.  You can execute the following once:

	echo "autoload -U compinit; compinit" >> ~/.zshrc

To load completions in your current shell session:

	source <(ltk completion zsh)

To load completions for every new session, execute once:

#### Linux:

	ltk completion zsh > "${fpath[1]}/_ltk"

#### macOS:

	ltk completion zsh > $(brew --prefix)/share/zsh/site-functions/_ltk

You will need to start a new shell for this setup to take effect.


```
ltk completion zsh [flags]
```

### Options

```
  -h, --help              help for zsh
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --format string   Output format: json, yaml, toml, text, or markdown (default "text")
```

### SEE ALSO

* [ltk completion](/ltk/reference/cli/ltk_completion/)	 - Generate the autocompletion script for the specified shell

