name: yaml/assert-count-fail
seams: [e2e]
kind: error
format: yaml
error: HEW011
error_seam: apply-ir
error_path: /mcpServers
patch_line: 6
message_contains: ["assertion-failed", "count"]
ops: [OP-27]
why: ytt's `expects="1"` generalized. `? count` is core; `? exhaustive` desugars to it.
spec: "§7.1, §11 OP-27, §11.10"
