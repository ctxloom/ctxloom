name: toml/surface-ambiguous
seams: [e2e]
kind: error
format: toml
error: HEW041
error_seam: apply-ir
error_path: /tool/ctxloom/timeout
patch_line: 6
message_contains: ["surface-ambiguity"]
ops: [OP-38]
why: |
  A path defined at two surfaces — which TOML forbids but real files occasionally
  contain. Hew refuses rather than picking one, because picking one silently
  orphans the other.
spec: "§8.4 rule 2"
