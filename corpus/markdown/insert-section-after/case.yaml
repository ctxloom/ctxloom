name: markdown/insert-section-after
seams: [e2e]
kind: ok
format: markdown
ops: [OP-43]
why: |
  Section insertion uses the same placement rule as every other container (§6.2).
  Blank lines are structural, not written: the patch carries none and the backend
  emits exactly one between inserted blocks.
spec: "§6.2, §8.6, §11 OP-43"
