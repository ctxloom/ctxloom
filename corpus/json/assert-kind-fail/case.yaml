name: json/assert-kind-fail
seams: [e2e]
kind: error
format: json
error: HEW011
error_seam: apply-ir
error_path: /mcpServers
patch_line: 6
message_contains: ["assertion-failed", "kind"]
ops: [OP-28]
why: |
  Derived from agent.InstallMCPServerJSON's measured refusal of an `mcpServers`
  key that is present but of the wrong type — and from B6, the codex TOML twin
  that lacks the guard. `? kind` makes that refusal expressible in a patch.
spec: "§7.1, §11 OP-28"
