# Owner-predicate prototype for the ctxloom/ltk/harp shared harness-config lib.
TOP := `git rev-parse --show-toplevel 2>/dev/null || echo .`

# Run the prototype's tests.
test:
    go test ./... -v

# Vet + build check.
check:
    go vet ./...
